//Using filter method
var filter = function(arr, fn) {
    return arr.filter(fn);
};
//without filter method
var filter = function(arr, fn) {
    var arr1=[];
    for(let i=0;i<arr.length;i++)
    {
        if(fn(arr[i],i)){
            arr1.push(arr[i]);
        }
    }
    return arr1;
};
